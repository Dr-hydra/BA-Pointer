<Window x:Class="BA.Pointer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BA Pointer"
        Width="980" Height="760"
        MinWidth="820" MinHeight="650"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource PageBackground}"
        FontFamily="Segoe UI"
        Closing="OnClosing"
        Closed="OnClosed">
    <Window.Resources>
        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,14,14" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Accent}" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Margin" Value="0,6,0,6" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#DCE8FA" />
            <Setter Property="Background" Value="#172031" />
            <Setter Property="BorderBrush" Value="#33425B" />
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#287EA7" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#58BDE4" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#243149" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="86" />
            <RowDefinition Height="*" />
            <RowDefinition Height="48" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#151E2C" BorderBrush="#2B3A50" BorderThickness="0,0,0,1" Padding="24,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Width="48" Height="48" Background="#26354B" BorderBrush="#4A6584" BorderThickness="1" CornerRadius="6">
                    <Image x:Name="HeaderCursorPreview" Width="36" Height="36" Stretch="Uniform" />
                </Border>
                <StackPanel Grid.Column="1" Margin="14,2,0,0">
                    <TextBlock Text="BA Pointer" FontSize="22" FontWeight="SemiBold" />
                    <TextBlock Text="桌面鼠标效果控制台" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,3,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="StartButton" Content="启动效果" Click="OnStartClick" Background="#237EA7" BorderBrush="#58BDE4" />
                    <Button x:Name="StopButton" Content="停止" Click="OnStopClick" />
                    <Button Content="退出" Click="OnExitClick" Margin="0" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" x:Name="SettingsTabs" Margin="20,16,20,8" Background="Transparent" BorderBrush="#34435D">
            <TabItem Header="效果参数">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,14,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource SectionBorder}">
                            <StackPanel>
                                <TextBlock Text="FX_Touch" Style="{StaticResource SectionTitle}" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="总体缩放" />
                                    <TextBlock x:Name="EffectScaleValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="EffectScaleSlider" Grid.ColumnSpan="2" Minimum="0.5" Maximum="2.5" TickFrequency="0.05" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="亮度 / 不透明度" />
                                    <TextBlock x:Name="EffectOpacityValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="EffectOpacitySlider" Grid.ColumnSpan="2" Minimum="0.1" Maximum="1" TickFrequency="0.05" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="动画时长倍率" />
                                    <TextBlock x:Name="EffectDurationValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="EffectDurationSlider" Grid.ColumnSpan="2" Minimum="0.5" Maximum="2.5" TickFrequency="0.05" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Style="{StaticResource SectionBorder}" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="Trail / Ring (4)" Style="{StaticResource SectionTitle}" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="拖尾宽度倍率" />
                                    <TextBlock x:Name="TrailWidthValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="TrailWidthSlider" Grid.ColumnSpan="2" Minimum="0.5" Maximum="4" TickFrequency="0.1" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="拖尾持续时间" />
                                    <TextBlock x:Name="TrailDurationValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="TrailDurationSlider" Grid.ColumnSpan="2" Minimum="100" Maximum="800" TickFrequency="10" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="移动碎片密度" />
                                    <TextBlock x:Name="DistanceEmissionValue" Grid.Column="1" Style="{StaticResource ValueText}" />
                                    <Slider x:Name="DistanceEmissionSlider" Grid.ColumnSpan="2" Minimum="0.25" Maximum="3" TickFrequency="0.05" ValueChanged="OnSliderValueChanged" Margin="0,24,0,0" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="资源与系统">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,14,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="310" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Style="{StaticResource SectionBorder}">
                            <StackPanel>
                                <TextBlock Text="光标素材" Style="{StaticResource SectionTitle}" />
                                <Border Height="170" Background="#0E141E" BorderBrush="#34435D" BorderThickness="1" CornerRadius="4" Margin="0,0,0,14">
                                    <Image x:Name="CursorPreview" Width="128" Height="128" Stretch="Uniform" />
                                </Border>
                                <TextBlock Text="PCIcon_MousePoint.png" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,0,0,12" />
                                <Button Content="恢复系统光标" Click="OnRestoreCursorClick" Margin="0" />
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <Border Style="{StaticResource SectionBorder}" Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="运行方式" Style="{StaticResource SectionTitle}" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Label Content="生效范围" Padding="0" />
                                        <ComboBox x:Name="TargetCombo" Grid.Column="1" SelectionChanged="OnComboSettingChanged">
                                            <ComboBoxItem Content="所有桌面" />
                                            <ComboBoxItem Content="仅 Blue Archive" />
                                        </ComboBox>
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="140"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                        <Label Content="刷新率" Padding="0" />
                                        <ComboBox x:Name="FpsCombo" Grid.Column="1" SelectionChanged="OnComboSettingChanged">
                                            <ComboBoxItem Content="60" />
                                            <ComboBoxItem Content="120" />
                                            <ComboBoxItem Content="144" />
                                        </ComboBox>
                                    </Grid>
                                    <CheckBox x:Name="SystemCursorCheck" Content="使用提取素材替换默认箭头" Checked="OnCheckSettingChanged" Unchecked="OnCheckSettingChanged" />
                                    <CheckBox x:Name="StartupCheck" Content="登录 Windows 后自动启动" Checked="OnCheckSettingChanged" Unchecked="OnCheckSettingChanged" />
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Background="#151E2C" BorderBrush="#2B3A50" BorderThickness="0,1,0,0" Padding="20,8">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Ellipse x:Name="StatusIndicator" Width="9" Height="9" Fill="#68778B" VerticalAlignment="Center" Margin="0,0,9,0" />
                <TextBlock x:Name="StatusText" Grid.Column="1" Text="就绪" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" />
                <Button Grid.Column="2" Content="应用并保存" Click="OnApplyClick" Margin="0" Padding="16,6" />
            </Grid>
        </Border>
    </Grid>
</Window>
